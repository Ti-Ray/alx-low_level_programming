#include "main.h"
/**
 * islower -This one displays one is c is lowercase
 *
 * Return: Always 0 (success)
 */
int _islower(int c)
{
	if (c != _islower(c))
	{
		_putchar('0');
	}
	else
	{
		_putchar('1');
	}
	return (0);
}
